const ApiKeys = () => {
  return (
    <div
      style={{
        border: "1px solid #ddd",
        padding: "20px",
        borderRadius: "8px",
        marginBottom: "20px",
      }}
    >
      <h2>API Keys</h2>

      <p>Manage API keys here.</p>
    </div>
  );
};

export default ApiKeys; 