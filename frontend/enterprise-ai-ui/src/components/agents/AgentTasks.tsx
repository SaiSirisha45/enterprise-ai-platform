const AgentTasks = () => {
  return (
    <div
      style={{
        marginTop: "20px",
        border: "1px solid #ddd",
        borderRadius: "8px",
        padding: "15px",
      }}
    >
      <h2>Running Tasks</h2>

      <p>No active tasks.</p>
    </div>
  );
};

export default AgentTasks; 